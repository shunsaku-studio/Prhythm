# minimal-deck

2-slide reference markup used by `assemble.sh --minimal`.

- `slides.html` — `<section>` blocks only (inserted into `index.html`)

```sh
bash skills/create-html-deck/scripts/assemble.sh slides/my-deck --minimal
```
