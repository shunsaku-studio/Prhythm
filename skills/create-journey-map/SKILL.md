---
name: create-journey-map
description: "ヒアリング情報・ペルソナ情報をもとにジャーニーマップ（JM）を生成するskill。「As-Is JMを作って」「ジャーニーマップを作って」「現状の体験を可視化して」「To-Be JMを作って」「理想の体験を描いて」などのリクエストで発動する。2つのモードを持つ。mode:asis — As-Is JM（台本形式）を生成し、インサイト抽出・HMW問いへの変換まで行う。mode:tobe — チームが合意したHMW優先度をもとにTo-Be JMを生成し、コアシーン候補を提示する。モードが指定されていない場合は mode:asis として実行する。"
disable-model-invocation: false
---

# ジャーニーマップ生成ガイド

ヒアリング情報をもとにジャーニーマップ（JM）の生成を支援する。2つのモードを持ち、サブコマンドとして使い分ける。

> 人間向けの概要・使い方は [`README.md`](README.md) を参照。

## モード一覧

| モード | コマンド例 | インプット | アウトプット | 詳細手順 |
|-------|-----------|-----------|-------------|---------|
| **mode:asis** | 「As-Is JMを作って」 | ヒアリングメモ、ペルソナ情報 | As-Is JM → インサイト → HMW | [`references/mode-asis.md`](references/mode-asis.md) |
| **mode:tobe** | 「To-Be JMを作って」 | As-Is JMのアウトプット＋HMW優先度 | To-Be JM → 対比サマリー → コアシーン候補 | [`references/mode-tobe.md`](references/mode-tobe.md) |

## モード判定ルール

- ユーザーの指示に「As-Is」「現状」「ヒアリングから」などが含まれる → **mode:asis**
- ユーザーの指示に「To-Be」「理想」「HMWをもとに」などが含まれる → **mode:tobe**
- モード指定がない場合 → **mode:asis** をデフォルトとする
- mode:tobe実行時にAs-Is JM・HMW優先度が渡されていない場合 → 実行せず、mode:asisの実行を促す

## なぜ2モードに分かれているか

As-Is→HMWとTo-Beの間には、チームの意思決定（どのHMWを優先するか）が必要になる。この判断をスキップしてTo-Beに進むと、現状の課題を起点としない「ウィッシュリスト的なTo-Be」が生まれるリスクがある。mode:asisは必ずHMW提示で停止し、チームの合意を待つ。

---

## JMの定義と役割

### ジャーニーマップとは

ユーザーの行動・感情・思考・状況を時系列で可視化するツール。核心は「As-Is（現状）とTo-Be（理想）の差分から、どこに介入すべきか（課題・機会）を特定すること」にある。

### よくある誤用（注意）

- ❌ 「プロトタイプで体験させたいコアシーンを特定するため」にJMを使う → コアシーンの特定はJMから導出される後段の作業であり、JM自体の目的ではない
- ❌ As-IsとTo-Beを同時に作る → As-Isの課題が見えてはじめて意味のあるTo-Beが描ける
- ❌ 感情を数値スコア（1〜5）で表現する → 定性的なJMでは数値より言葉で表現した方がリアルになる

---

## 共通ルール

以下のルールはmode:asis・mode:tobeの両方に適用する。各モードの詳細手順に入る前に必ず読む。

### 台本形式の記述ガイドライン

- 演劇の台本のように、そのペルソナのシチュエーションをありありと描く
- 状況描写は五感に訴えるレベルで書く（場所、時間帯、周囲の様子）
- 発話は「本当にこの人が言いそうな言葉」にする
- 感情は数値スコアではなく言葉で表現する
- ヒアリング情報に含まれるエピソード・発言は最大限活用する
- 推測で補った箇所は `※推測` と明示する

### フェーズの設計ルール

- フェーズ数は固定しない。そのペルソナの体験を描くのに必要な数だけ設ける
- 粒度は大きめでよい（「認知」「初回利用」「継続」など）
- ペルソナの行動や感情をありありと思い描けることが最優先

### 各フェーズの共通構造

```
【フェーズ名】

状況：
  その人がいる場所・時間帯・周囲の環境を具体的に描写する。
  映像として頭に浮かぶレベルの解像度で書く。

発話 / 内心の声：
  その人が実際に口にする言葉、または頭の中のつぶやきを一人称で書く。

行動：
  その場面でとる具体的なアクションを、順序がわかるように書く。

感情：
  感情の変化を言葉で表現する。数値スコアは使わない。

タッチポイント：
  この場面で接触するサービス・画面・人・モノを列挙する。
```

mode:asisでは上記に `課題：` を、mode:tobeでは `As-Isからの変化：` を各フェーズに追加する（詳細は各モードファイル参照）。

### アウトプット形式

- メインアウトプット：台本形式のMarkdownテキスト
- オプション（ユーザーの要望に応じて出力）：
  - フェーズ一覧のMarkdownテーブル（Notion・Confluenceなどに貼る用）
  - CSV形式（FigJam・Miroへのインポート用）

---

## 実行フロー

1. **モードを判定する**（上記「モード判定ルール」）
2. **共通ルールを確認する**（本ファイル）
3. **該当モードの詳細手順を読み、実行する**
   - mode:asis → [`references/mode-asis.md`](references/mode-asis.md)
   - mode:tobe → [`references/mode-tobe.md`](references/mode-tobe.md)
4. **各モードのチェックリストで自己検証してから完了する**

mode:asisはHMW提示後に必ず停止する。mode:tobeはコアシーン候補提示後に次のステップを案内する。
