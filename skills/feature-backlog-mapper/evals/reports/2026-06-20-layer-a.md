# Layer A — Self-review report

- Date: 2026-06-20
- Skill version: pre-commit on `feat/feature-backlog-mapper-skill` (base ee38765)
- Reviewer: skill author (self-review)

## 1. validate-skill.sh

```
Summary: 0 fail, 0 warn
PASS: Frontmatter opening present
PASS: Frontmatter has name: feature-backlog-mapper
PASS: Frontmatter has description (437 chars)
PASS: Frontmatter has disable-model-invocation: false
PASS: name matches directory
PASS: SKILL.md line count OK (144)
PASS: No common hedging patterns
PASS: No Windows-style paths
PASS: README.md exists
PASS: README has all 8 required headings
PASS: Link check completed
```

Exit 0.

## 2. 5-dimension Layer A scoring

| Dim | Score | Note |
|-----|-------|------|
| Discoverability (20%) | 5 | name == dir; description is third-person, states WHAT (Map use cases to a prioritized feature inventory or sprint-ready product backlog) and WHEN (機能一覧 / 要件定義書 / プロダクトバックログ / MoSCoW / story / acceptance を含む依頼) with concrete trigger phrases in JA + EN |
| Conciseness (25%) | 5 | SKILL.md 144 行（推奨 ≤300 内）/ 教科書解説なし / details delegated to references/ via Reference router table |
| Instruction quality (25%) | 5 | imperative throughout / If→Then in Step 0 / explicit MUST and NEVER blocks / no hedging (validated) |
| Structure (20%) | 5 | progressive disclosure SKILL → references → evals; one-level link depth; forward slashes; scripts state execute intent |
| Observability (10%) | 5 | each Step has artefact (output paths); Step 5 emits a structured report block; Layer A/B/C rubric pinned in evals/rubric.md |

Weighted: `(5×0.20 + 5×0.25 + 5×0.25 + 5×0.20 + 5×0.10) / 1.00 = 5.0`

## 3. README rubric (per readme-template + review-rubric)

| Item | Score | Evidence |
|------|-------|----------|
| 利用メリット | 5 | All bullets are user/team outcomes ("機能の根拠が明確になる" / "優先度を一緒に運べる" / "作り直しがいらない" / "見積もりと開発の入口になる" / "棄却の証跡が残る") — no implementation detail leaked |
| 利用シーン | 5 | Aligned with メリット 1:1; situations and pains, not commands; trailing 依頼の例 block in 使い方 |

## 4. Findings

| Severity | Finding | Action |
|----------|---------|--------|
| Strength | description packs JA + EN trigger terms in one block — high discoverability across language toggles | keep |
| Strength | NEVER list pins the four most likely failure modes at the top of the file (UC anchor, Must rationale, Won't isolation, UC ID column, AC/INVEST omission) | keep |
| Strength | Reference router table places the 10 references into "Task → Doc" form; agents can stay shallow on SKILL.md and pull only what's needed | keep |
| Suggestion | None this pass | — |
| Critical | None | — |

## 5. Verdict

**Approve.** All scored dimensions ≥ 4 (in fact all 5). Proceed to Layer B.
